# Duration curve table for a given vector

The Duration Curve (DC) represents the percentage of time that a
specific value of a vector has been used in a specific time period.

## Usage

``` r
get_duration_curve(vct)
```

## Arguments

- vct:

  numeric vector

## Value

tibble
