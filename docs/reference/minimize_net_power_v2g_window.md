# Minimisation of net power with bidirectional flexibility (single window)

Minimisation of net power with bidirectional flexibility (single window)

## Usage

``` r
minimize_net_power_v2g_window(
  G,
  LF,
  LS,
  direction,
  time_horizon,
  LFmax,
  import_capacity,
  export_capacity,
  lambda = 0
)
```
