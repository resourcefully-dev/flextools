# `print` method for `SmartCharging` object class

`print` method for `SmartCharging` object class

## Usage

``` r
# S3 method for class 'SmartCharging'
print(x, ...)
```

## Arguments

- x:

  `SmartCharging` object returned by `smart_charging` function

- ...:

  further arguments passed to or from other methods.

## Value

nothing but prints information about the `SmartCharging` object
