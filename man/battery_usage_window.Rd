% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/battery_optimization.R
\name{battery_usage_window}
\alias{battery_usage_window}
\title{Minimize battery usage (just a window)}
\usage{
battery_usage_window(
  G,
  L,
  Bcap,
  Bc,
  Bd,
  SOCmin,
  SOCmax,
  SOCini,
  import_capacity,
  export_capacity,
  lambda = 0
)
}
\arguments{
\item{G}{numeric vector, renewable generation profile}

\item{L}{numeric vector, static load profile}

\item{Bcap}{numeric, battery capacity in energy units (kWh * slots/h)}

\item{Bc}{numeric, maximum charging power (kW)}

\item{Bd}{numeric, maximum discharging power (kW)}

\item{SOCmin}{numeric, minimum State-of-Charge (\%)}

\item{SOCmax}{numeric, maximum State-of-Charge (\%)}

\item{SOCini}{numeric, initial State-of-Charge (\%)}

\item{import_capacity}{numeric vector, maximum grid import (kW)}

\item{export_capacity}{numeric vector, maximum grid export (kW)}

\item{lambda}{numeric, ramping penalty weight.}
}
\value{
numeric vector
}
\description{
The capacity is enforced by \code{battery_solve_grid_window()}'s constraints, so
the objective is free to ask for the \emph{least battery} that satisfies them:
\verb{min sum(B^2)}, with no linear term pulling the net flow anywhere. The
quadratic penalises power, so the discharge settles exactly on the binding
capacity rather than below it, and the energy neutrality it has to give back
is recharged at the lowest power the window allows.
}
\details{
This is the opposite reading of the same constraints from
\code{battery_grid_window()}, whose \code{q = 2 * (L - G)} makes the objective
\code{sum((L - G + B)^2)} and therefore flattens the whole profile.
}
\keyword{internal}
