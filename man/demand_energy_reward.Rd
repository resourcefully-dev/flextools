% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand_optimization.R
\name{demand_energy_reward}
\alias{demand_energy_reward}
\title{Reward per unit of optimized load kept}
\usage{
demand_energy_reward(P, q, envelope)
}
\arguments{
\item{P}{numeric matrix or \code{NULL}, normalised quadratic term.}

\item{q}{numeric vector, linear term.}

\item{envelope}{numeric, bound on the absolute value of any decision
variable in the feasible box (kW).}
}
\value{
numeric scalar
}
\description{
When the energy row is a range, the optimizer must be told to keep energy
rather than drop it: every objective in this file improves when load is
removed (less net flow, less cost). A uniform linear reward on \code{O} that
strictly dominates the objective's marginal gain from removing one unit —
\verb{|d/dO| <= max row sum of |P| * max |X| + max |q|} anywhere in the feasible
box — makes the solver keep as much energy as the constraints admit and
shape it afterwards. Being the same in every slot it does not bias where the
energy goes, only how much of it stays.
}
\keyword{internal}
